Чтобы начать работу с БД, нужно написать:
```python
from Database.api import Session, DataBase  
with Session() as session:
   db = DataBase(session)
```












## Database/methods - папка со всеми методами, связанными с БД
## Database/main.py - создание класса, связвывающего все методы с БД (DataBase)

## DataBase.user - класс UsersRequests
### Методы:
  * add (добавление пользователя в таблицу, если usrname, pthone и email уникальны)
    - name
    - username
    - lastnname
    - birthday
    - avatar_url
    - phone
    - email
  * select_all (получение всей инфы обо всех пользователях)
    - Нет параметров
  * update (Обновление у пользователя с id = id атрибут attr_name на value)
    - id (того, у которого нужно что-то изменить)
    - attr_name (имя атребута, который нужно обновить)
    - value (значение, на которое нужно изменить запись)
   * delete (Удаление пользователя)
     - id (того, кого нужно уничтожить из БД)
   
### Системные методы:
  * _check_unique_attr (проверка уникальности значения)
    - attr_name (имя колонки)
    - value (провверяемое значение)

(Дальше не делал. Если не лень, то напишите, пажалуйста, методы, которые вам нужны)
## DataBase.chats - класс ChatssRequests
### Методы:
### Системные методы:

## DataBase.messages - класс MessagesRequests
### Методы:
### Системные методы:

## DataBase.participants - класс ParticipantsRequests
### Методы:
### Системные методы:
