# Описание необходимых изменений/добавлений на сервер
ВАЖНО: в возврате написана минимальная нужная инфа на данный момент, но её может стать больше, и если БД возвращает больше, то лучше вернуть всю информацию
- Уникальное имя пользователя - username: str
- Отображаемое имя пользователя - name: str
- Уникальный номер - id: int
- Тип - type: str
- Имя (name) отправителя - sender_name: str
- Ссылка на аватар - avatar_url: str
- Пользователь-участник - участник чата, информация о котором, тем не менее, берётся из таблицы [users](../../Database/models/users.py)
- Список пользователей-участников чата - participants: list(dict)
## Изменения
* Заменить в коде сервера использование username на id
  - ```python
    # Пример из Server.auth
    if out.get("data") :
        out = self.db.select_user_by_username(username=username)
        user_id = out.get("data").get("id") # Получаем id пользователя

    await websocket.send(json.dumps({"id_task": id_task, "response": out}))
    if user_id: # Проверяем наличие user_id
      self.connected_clients[user_id] = websocket # Сохраняем клиента по id
    ```
* Добавить в auth обработку такого сценария где пользователя уже существует в connected_clients c закрытием соединения

* Добавить проверки на isSuccess перед получением data в registration и auth

* Добавить отправку push-уведомлений в send_message и create_chat

* Отслеживать в handler user_id текущего пользователя и удалять отключившихся пользователей
 
## Добавления
* Добавить метод отправки push-уведомлений
  - **Принимает**: user_id, текст уведомления: str
  - **Возвращает**: None

* Добавить метод отправки push-уведомлений всем участникам чата кроме отправителя (с использованием предыдущего метода)
  - **Принимает**: chat_id, sender_id, текст уведомления: str
  - **Возвращает**: None
